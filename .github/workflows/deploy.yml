# .github/workflows/deploy.yml
name: CI/CD to EC2

on:
  push:
    branches:
      - main
      - qa

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout código
      uses: actions/checkout@v3

    - name: Loguear en Docker Hub
      uses: docker/login-action@v2
      with:
        username: ${{ secrets.DOCKER_USERNAME }}
        password: ${{ secrets.DOCKER_PASSWORD }}

    - name: Construir imagen y subir a Docker Hub
      run: |
        docker build -t arieling/ms-user-preferences .
        docker push arieling/ms-user-preferences

    - name: Acceder a EC2 y desplegar contenedor
      uses: appleboy/ssh-action@v1.0.3
      with:
        host: ${{ secrets.EC2_HOST }}
        username: ${{ secrets.EC2_USER }}
        key: ${{ secrets.EC2_SSH_KEY }}
        script: |
          echo "${{ secrets.ENV_FILE }}" > .env
          docker pull arieling/ms-user-preferences
          docker stop ms-user-preferences || true
          docker rm ms-user-preferences || true
          docker run -d --name ms-user-preferences -p 4004:4004 --env-file .env arieling/ms-user-preferences
